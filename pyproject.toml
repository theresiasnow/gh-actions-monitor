[project]
name = "gh-actions-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
    "typer>=0.25.0",
]

[project.scripts]
gh-monitor = "main:app"

[tool.uv]
package = true
